export function marquee() {
 window.onload = function () {
  const marquees = document.querySelectorAll('.slider__li')
  const container = document.querySelector('.slider__list')
  const containerWidth = container.offsetWidth
 }
}
// marquee()
